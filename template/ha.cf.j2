logfacility     local0
keepalive       2
deadtime        10
warntime        5
initdead        20

udpport         694
bcast           {{ vip_interface }}

auto_failback   on

# Nombres de nodo tal como aparecen en el inventario / hostname
{% for host in groups['cache'] %}
node {{ host }}
{% endfor %}